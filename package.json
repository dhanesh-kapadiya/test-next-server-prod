{
  "name": "custom-server-express",
  "version": "1.0.0",
  "scripts": {
    "dev": "node server.js",
    "build": "next build",
    "start": "npm run build && NODE_ENV=production node server.js -p $port"
  },
  "dependencies": {
    "express": "^4.14.0",
    "next": "latest",
    "react": "^16.4.1",
    "react-dom": "^16.0.0"
  }
}
